# トレさく デザインガイド

> 2026-07-31 全面改訂。実装（app/ + src/ + generateHtml.ts）を一次ソースとして照合し、
> 乖離していた記述を実態に合わせた。実装とこのガイドが食い違ったら、どちらが正か判断して両方を直すこと。

---

## 0. 前提 — ユーザーは2人いる

このアプリのデザインは、**操作する人と読む人が別人**であることを常に前提にする。

| | 画面（アプリUI） | 紙（PDF出力） |
|---|---|---|
| 誰のもの | **医療職**（PT/OT/ST・看護・介護） | **患者**（多くは高齢者） |
| 最適化する方向 | 速さ・情報密度・片手操作 | 大きさ・明快さ・一覧性 |
| 文字サイズ | 13〜15px 中心（密度優先） | 本文14pt〜・処方数値28pt（読みやすさ優先） |
| 操作 | 業務アプリとして普通の操作でよい | 操作なし。紙として完結 |

「高齢者への配慮」は**PDF側の要件**であり、画面UIを大きく・単純にする理由にはならない。
逆に画面UIの密度を上げても、PDFの読みやすさを犠牲にしてはならない。

## 1. デザイン原則

1. **3タップ完結** — 選択 → 調整 → 出力。画面遷移は最小限
2. **リストが主役** — 100種のカタログが商品。検索・フィルタ等の道具はリストの面積を奪わない
3. **紙が成果物** — 画面はあくまで作業場。PDF出力時の見栄えを常に確認してから変更を確定する
4. **装飾より明快さ** — 色数・影・アニメーションを増やさない。余白とウェイトで階層を作る
5. **日本語ファースト** — UIテキスト・行間・折り返しは日本語基準。英名は補助情報
6. **効能表現禁止**（CLAUDE.md 遵守） — 「治る」「効く」等は画面・PDF・空状態の文言すべてで不可

## 2. カラーシステム

### 2.1 トークン（tailwind.config.js）

ブランド色は **navy `#0B2545`**。アクション・選択状態・PDF見出しすべて navy で統一する。

| トークン | HEX | 役割 |
|---|---|---|
| `navy` | `#0B2545` | ブランド色。CTA、選択状態、PDF見出し・処方枠 |
| `teal` | `#0F766E` | ターゲット筋のアクセント（ピル・PDFタグ） |
| `teal-soft` | `#E6F4F2` | teal の背景 |
| `warn` | `#B91C1C` | 注意・削除・破壊的操作 |
| `warn-soft` | `#FBEAEA` | 注意ボックスの背景 |
| `ink` | `#0F172A` | 本文・見出し |
| `ink2` | `#475569` | 補足テキスト |
| `ink3` | `#94A3B8` | プレースホルダ・微弱テキスト・セクションラベル |
| `line` | `#E6EAF0` | 枠線・区切り線 |
| `surface` | `#F5F7FA` | 画面背景 |
| `card` | `#FFFFFF` | カード・バー背景 |
| `primary` / `primary-soft` | `#1D4ED8` / `#EEF3FF` | **ほぼ未使用。新規使用禁止**（navy に統一。将来 config から削除） |

### 2.2 直書きで定着している準トークン

実装内で繰り返し使われている hex。**新規箇所ではこの表の用途どおりに使う**。
トークン化（tailwind.config への追加）は改修時にまとめて行う。

| HEX | 事実上の役割 | 主な使用箇所 |
|---|---|---|
| `#F4F6FA` | 入力欄・小ボタンの背景 | 検索バー、TextInput、ステッパー |
| `#EEF2F9` | navy の soft 背景 | 姿勢ピル、番号バッジ、PDF目的欄 |
| `#FAFBFD` | 帯・リスト内背景 | 処方帯、ポイント枠 |
| `#C7D7F5` | 選択状態の枠線 | 選択中カードの border |
| `#CBD5E1` | 未選択チェックボックスの枠 | ExerciseCard |
| `#F5D2D2` | warn の枠線 | 注意メモ、削除ボタン |
| `#7F1D1D` | warn の本文色 | 注意メモの入力テキスト、PDF注意文 |

### 2.3 ルール

- **新しい色を足さない**。上記2表にない hex を書きたくなったら、まず既存トークンで代替できないか検討する
- 選択状態は「チェック + 枠色 + 背景」の3重表示（色だけに依存しない）
- ⚠️ 既知の逸脱: print.tsx のツールバーボタン `#0EA5E9`（空色）はパレット外。navy に修正予定

## 3. タイポグラフィ

### 3.1 画面（px）

| 要素 | サイズ | ウェイト | 用途 |
|---|---|---|---|
| 画面タイトル | 26px | Bold | 「運動ライブラリ」 |
| カード見出し | 15px | Bold | 運動名（一覧） / 14.5px（編集カード） |
| 本文・入力 | 13〜14px | Regular | 説明、TextInput、ピル |
| セクションラベル | 12〜13px | Semibold + `tracking-widest` | 「部位」「実施頻度」等 |
| 処方数値 | 20px (`text-xl`) | Bold | ステッパーの値 |

- フォント: システム（iOS: Hiragino Sans / Android: Noto Sans JP）
- セクションラベルの `tracking-widest` はこのアプリの識別子。ラベルには必ず付ける

### 3.2 PDF（pt）

| 要素 | サイズ | 備考 |
|---|---|---|
| 表紙タイトル | 28pt Bold navy | |
| 運動名 | 24pt Bold ink | |
| 処方数値（回数・セット等） | **28pt Bold navy** | 患者が最初に見る数字。最大サイズ |
| ポイント本文 | 14pt / 行間1.5 | 高齢者の可読下限を14ptとする |
| 注意文 | 13pt Medium `#7F1D1D` | |
| 表紙テーブル・フッター | 10〜11pt | 一覧性優先の例外 |

- フォント: `Hiragino Kaku Gothic ProN, Noto Sans JP` を明示指定
- **患者が読む本文を14pt未満にしない**（表紙の一覧テーブルのみ例外）

## 4. 形状・エレベーション

| 要素 | 値 |
|---|---|
| カード角丸 | 14px |
| カード内要素（サムネ枠・入力欄・帯） | 9〜11px |
| 小ボタン・バッジ | 8px / ピルは `rounded-full` |
| CTA影 | `shadowColor: navy, opacity 0.35, radius 16, offset (0,8)` |
| カード影 | ごく弱く（opacity 0.03〜0.08）。基本は border で区切る |
| 基本パディング | 画面横 16〜20px、カード内 12px（`p-3`） |

エレベーションは「下部固定CTA だけが浮いている」状態を保つ。カードに強い影を足さない。

## 5. コンポーネントカタログ

### Pill（属性バッジ）
3トーン: `navy`（姿勢 / bg `#EEF2F9`）・`neutral`（部位 / bg `#F1F5F9`）・`teal`（ターゲット筋 / bg `teal-soft`）。
13px Semibold、`rounded-md px-2 py-0.5`。トーンと属性の対応を崩さない。

### FilterBar（フィルタピル）
`rounded-full px-3.5 py-2`、選択中 `bg-navy text-white`、非選択 `border-line bg-card`。
⚠️ 現状は横スクロールで「四つ這い」「呼吸」が初期表示外。改修対象（§9）。

### ExerciseCard(一覧カード)
サムネ92×72 + 運動名 + 英名 + ピル3種 + チェックボックス26px。
選択中: `border-#C7D7F5` + サムネ背景 `primary-soft` + navyチェック。タップ領域はカード全面。

### ParamCell（±ステッパー）
ラベル12px → 値20px Bold → ±ボタン32px の縦積み。
⚠️ ボタン32pxは44px基準未達。長押し連続増減もなし。改修対象（§9）。

### BottomActionBar（下部固定CTA）
`h-[56px] rounded-[14px] bg-navy` + CTA影 + 白文字Bold。件数はバッジ（`bg-white/20`）で表示。
実行中は `bg-ink3` + オーバーレイ（黒40% + 白カード + ActivityIndicator）。

### TextInput（共通スタイル)
`rounded-lg border-line bg-[#F4F6FA] text-[13px]`、プレースホルダ `#94A3B8`。
注意メモのみ warn 系: `bg-warn-soft border-[#F5D2D2]` + 左端に `w-1 bg-warn` の縦バー、文字 `#7F1D1D`。

### トグル（縦向き/横向き）
FilterBar と同形のピル2択。同じ見た目は同じ挙動にする（新しいトグル形状を発明しない）。

### 空状態
絵文字1つ + Bold見出し + 補足 + navy のアクションボタン。preview.tsx の「運動が選択されていません」が基準形。

## 6. 画面レイアウト

```
[運動ライブラリ]                    [指導書プレビュー]
┌ ヘッダー(ロゴ/ご意見) ──────┐    ┌ 件数 ・ すべて解除 ────────┐
│ タイトル + 件数            │    │ 指導書の目的(任意)          │
├ 検索バー ──────────────────┤    ├ 編集カード × N ────────────┤
├ フィルタ(部位/姿勢/種類) ──┤    │  番号+▲▼ サムネ 運動名 開閉 │
├ ExerciseCard × N ──────────┤    │  処方帯: 回数/セット/保持/休息│
│  (スクロール)              │    │  展開: 目的/頻度/ポイント/注意│
├────────────────────────────┤    ├────────────────────────────┤
│ [指導書を作成 (N)] 固定CTA │    │ 縦/横トグル + [PDF出力] CTA │
└────────────────────────────┘    └────────────────────────────┘
```

- 一覧の選択数が1以上になったらCTAが出現（高さ約100px分を `paddingBottom` で確保）
- Web版のみ `/print` 画面あり: generateHtml の出力 + 固定ツールバー（戻る / 印刷）

## 7. PDF出力デザイン

構成: **表紙1枚 + 1運動1枚（+ 任意で実施チェック表）**。`@page A4 margin:10mm`、横向きは `.landscape` クラスで各要素を圧縮。

- **表紙**: センタータイトル(28pt) + 3px navy下線 → 目的欄（navy-soft帯）→ 発行日 → プログラム一覧テーブル（navyヘッダ行）→ 共通注意ボックス
- **運動ページ**: ヘッダー（タイトル+ページバッジ）→ タグ（姿勢navy/ターゲットteal）→ 運動名24pt → イラスト（高さ220px・横向き150px）→ **処方ストリップ**（navy 2px枠、ラベル白抜き、数値28pt）→ ポイント（番号バッジ+区切線）→ 注意ボックス（左5px赤バー）→ フッター
- **実施チェック表**（任意・デフォルトOFF）: 最終ページに4週分。行=運動 × 列=月〜日、土`#93C5FD`/日`#FCA5A5`。
  各行は「運動名 + 処方」を1行に収める（`nowrap` + `ellipsis` で行の高さを固定）。
  週の日付は手書き用の空欄、末尾にメモ欄と「次回お持ちください」（warn色）。
  収まる週数を `renderCheckSheets` の係数から見積もり、超える場合はシート分割。
  横向きは `.weeks` を2列flexにして縦を節約する。**要素の高さを変えたら係数の再較正が必要**

### ⚠️ 印刷可能高は A4 の理論値ではない（重要）

ブラウザ印刷（Web版）は URL・日付などを付加するため、**実効の印刷領域は A4 の理論値より狭い**。

#### 現物の高さ（2026-08-05 実ブラウザで実測）

| ページ種別 | 縦 | 横 |
|---|---|---|
| 運動ページ | **736px**（種目数によらず一定） | **619px** |
| 表紙 | **449 + 27.6 × 種目数**（10種目=725px / 20種目=1001px） | **401 + 24.6 × 種目数**（10種目=647px） |
| 実施チェック表 | budget 700 内 | budget 480 内 |

#### 上限は未確定（要実機確認）

`renderCheckSheets` の budget と `check-print-layout.mjs` の `PROVEN_SAFE` は
**縦700 / 横480** だが、この数字の根拠は「運動ページ（縦700px）は1枚に収まっている」
という観測だった。上表のとおり**運動ページは実際には736px**で、根拠が現物と合っていない。

- 縦791pxのチェック表でフッターだけが次ページに落ちた、という観測は有効。
  つまり縦の実効領域は **736〜791px の間**にある（736は破綻報告なしで運用中のため）
- 横は運動ページ619pxが運用中なので、実効領域は **619px以上**。「約490px」は誤り
- **表紙は縦10種目（725px）で既に700pxを超えている。** 実運用は10種目程度なので
  直ちに問題にはならないが、多種目の指導書では表紙が2ページに割れる可能性がある

#### 上限を確定させる手順（実機で1回だけ）

1. Web版で **3種目・チェック表なし・縦向き** の指導書を作り、`/print` から印刷 → PDFに保存
2. 同じものを **20種目** で作って印刷
3. それぞれ「運動ページが1枚に収まっているか」「表紙が2ページに割れていないか」を見る
4. 結果を `PROVEN_SAFE` と本節に反映する

#### その他

- **新しいページ種別を足すときは、この実効値を上限として設計する**
- **要素を足し引きしたら実測し直し、`check-print-layout.mjs` の `MEASURED` を更新する**
  （ガードはレンダリングできないので、値は手で記録している）
- 発行者情報は表紙フッターに `nowrap` で1行。折り返すと表紙が伸びるため入力を18/10文字に制限
- 休息は `restSeconds > 0` のときのみ表示。頻度は `white-space: nowrap`（折り返し防止済み）
- `page-break-inside: avoid` を維持。1ページに収まらない変更をしない

⚠️ **既知バグ**: `<head>` に viewport 指定がなく、スマホの `/print` 画面が横に破綻する。修正予定（§9）。

## 8. イラスト

- 形式: WebP（`assets/illustrations/*.webp`、PNG原本は `originals/`）
- スタイル: セミリアリスティックなフラットイラスト。白背景・影なし、中年男性・白Tシャツ+紺ハーフパンツ、細い黒輪郭線、動作矢印は赤
- 生成: gpt-image-1.5 / high。プロンプトは `assets/illustrations/PROMPT_TEMPLATE.md`、手順は `docs/dev-notes.md`
- 画面ではサムネイル枠内に `resizeMode: contain`。トリミングしない

## 9. アクセシビリティ — 目標と現状

目標（変更なし）:
- タップターゲット **44×44px 以上**
- コントラスト: 本文 ink/白 = 17:1 を維持。12:1 未満に下げない
- 全 Pressable に `accessibilityLabel` / 状態は `accessibilityState` で通知
- 選択状態は色+形+アイコンの3重表示

**改修バックログ**（施策IDは `docs/ui-ux-redesign.md` に対応）:

| # | 問題 | 場所 | 状態 |
|---|---|---|---|
| 1 | スマホで `/print` が横破綻（viewport欠落） | print.tsx / generateHtml.ts | ✅ B-6 完了 |
| 2 | フィルタ横スクロールで「四つ這い」「呼吸」が視界外 + リスト初期表示3件のみ | index.tsx | ⏳ A-1（バッチ2） |
| 3 | ステッパー32px・並べ替え▲▼20px が44px未達 | preview.tsx | ✅ B-1 / B-2 完了 |
| 4 | 選択済み運動を一覧できる手段がCTAバッジのみ | index.tsx | ⏳ A-3（バッチ2） |
| 5 | プログレスバー3本が無説明 / 画面名とのズレ | preview.tsx | ✅ B-4 完了 |
| 6 | 削除ボタンが孤立した✕1個で空白が広い | preview.tsx | ✅ B-3 完了 |
| 7 | print ツールバーの `#0EA5E9` がパレット外 | print.tsx | ✅ B-6 で修正 |
| 8 | 一覧カードのチェックボックス26px（タップ領域はカード全面のため実害なし） | ExerciseCard.tsx | 保留 |

**既知の制約**: デスクトップでウィンドウ幅を794px未満にすると `/print` に横スクロールが出る
（viewport meta を無視する環境 + A4固定幅のため）。PDFビューアと同じ挙動として許容。

## 10. 実装規約

- スタイルは NativeWind クラスで直接指定（StyleSheet 不使用）。影など className 不可のものだけ `style`
- 色は §2 の2表から選ぶ。**新規の直書き hex 禁止**
- 新コンポーネントは §5 の既存形状（角丸14px、ピル、soft背景）を流用してから発明する
- UI変更をしたら: `npx tsc --noEmit` → 画面確認 → **PDF出力確認**（縦・横両方）までがワンセット
